# Task List: Phase 8 Two-Column Layout & Data Filtering

- [x] `src/ui/app_window.py`: Refactor layout to use a master `QHBoxLayout` separating Controls/Mapping (Left) and Database Query (Right).
- [x] `src/ui/app_window.py`: Add `Exclude Filter Target` Toolbars (Column and Exact Match Value) inside Data Mapping.
- [x] `src/core/excel_loader.py`: Revamp numeric cleaning engine inside `_clean()` to aggressively drop `NaN` IDs and cast floats ending in `.0` strictly into integers.
- [x] `src/core/excel_loader.py`: Add runtime exclusion logic targeting `exclude_col` holding matching `exclude_val` inside the `load()` method.
- [x] `src/ui/workers.py`: Add `exclude_col/val` arguments handling inside `OrchestratorThread`.
- [x] `src/main.py`: Update AuditorApp signal relay fetching the exclusion components from ControlHub.
- [x] Verify application boot cleanly in new format.

---

# Future Task List: Next Major Milestones

## Phase 9: Offline AI Copilot
- [ ] Evaluate LLM Runners: Compare `llama.cpp` python bindings vs standalone `Ollama` process.
- [ ] UI Update: Build collapsible Copilot Sidebar in ControlHub alongside the right-side SQL area.
- [ ] Pre-Flight Logic: Hook the Copilot to the "Start Audit" button to review `Settings.yaml`, `SQL text`, and `Excel headers` for mismatches before proceeding.
- [ ] Local Test: Ensure the 8B-parameter instruction model responds in <3 seconds without lagging the main UI thread (use QThread).

## Phase 10: Visual Automation Configurator
- [ ] UI Update: Add "Settings / Macro Builder" button to header.
- [ ] UI Update: Create `QDialog` interface to construct locators (`search_box_id`) and wait delays visually.
- [ ] Configuration Serialization: Rewrite the Settings saver to serialize the UI entries securely back into `src/config/settings.yaml`.
- [ ] Hot-Reload Logic: Ensure `DesktopBot` instances reload the memory YAML configurations if the user saves a new visual mapping during runtime.
