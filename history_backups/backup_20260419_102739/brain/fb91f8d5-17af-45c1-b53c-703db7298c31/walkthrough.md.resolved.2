# Desktop Auditor v1.0 — Phase 7 Delivery
> Cross-system data validation & automated PostgreSQL generation with Dynamic UI.

## 🚀 The Dynamic UI Update is Live!

We've successfully bypassed the hardcoded `settings.yaml` dependencies by building powerful, responsive interfaces directly onto your Control Hub. The `Desktop Auditor` is now incredibly flexible.

### 1. 📊 Automated Target Data Mapping
We've added a new **Target Data Mapping** section directly under the *Excel Source File* picker.
- **Header Parsing:** Whenever you browse and select an `.xlsx` file, the system instantaneously and silently reads the exact row headers.
- **Dynamic Dropdowns:** It populates the "Target ID Column" and "Expected Value Column" dropdowns immediately, letting you manually map the ID (e.g. `committee_referrals_id`) rather than needing to dig into back-end configuration files. 

### 2. 💾 PostgreSQL Template parsing 
We built an intelligent **Database Query** widget located right above the *Audit Controls*. 
Instead of mapping Tables and Rows individually via `settings.yaml`, we implemented a smart Regex parsing module:

- Just paste whichever **"CURRENT"** query the team uses! 
- *For example:* Simply pasting `UPDATE committee_referrals SET committee_term_id = 9 WHERE committee_referrals_id = 2747;` triggers our real-time regex scanner.
- The UI auto-extracts your `table_name` (`committee_referrals`), target column, and ID bounds securely, funneling them straight into the massive payload generators! No manual typing required.

### 3. Sleek Two-Column Developer Layout
To ensure total visibility, we completely refactored the UI from an awkward stacked stack into a sleek split-screen layout.
- The **Left Panel** holds all your configurations, target mappings, and strict execution controls. 
- The **Right Panel** is exclusively dedicated to the `Current Database Query` mapping so you can freely read, edit, and paste enormous raw SQL batches seamlessly.

### 4. Advanced Data Integrity Engine 🛡️
We reinforced how `pandas` translates data to handle strange Excel formatting without crashing:
- **Automatic NaN ID Purging:** If an Excel cell comes up blank empty inside your ID columns, the loader instantaneously detects and drops it.
- **Float Sanitization:** Mathematical decimal conversion (e.g., getting `4977.0` instead of `4977` due to Python float casting) is strictly detected and forced safely back to integers so that your UI matching always matches the target database without syntax errors!
- **Data Exclusion Filtering:** The Data Mapping toolbar now hosts a feature to skip records! If your team uses "DOUBLE CHECKED" equals "TAMA", simply select that column and insert `TAMA` and the application will instantly strip out thousands of finished records immediately at boot time!

---
### Next Steps
1. Boot the application with: `python src/main.py`.
2. Notice the new wide, snappy right-placed Database view! 
3. Try selecting the optional target `DOUBLE CHECKED` column and input `TAMA` to see the engine auto-skip those matched database entries cleanly.
